apiVersion: v3
kind: ConfigMap
metadata:
  name: nginx
  namespace: {{ .Values.namespace }}

data:
  index.html: |
        <html>
    <h1>Welcome</h1>
    </br>
    <h1>Hi! I got deployed in {{ .Values.env.name }} Environment using Helm Chart </h1>
    </html
